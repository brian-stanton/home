---
# One source tree on every machine. Clone once (update: false) so later runs
# never reset your checkout. Install into ~/.local so `make install` does not
# need sudo (and cannot root-own the build dir).
- name: Check for Xcode command line tools
  ansible.builtin.command: xcode-select -p
  register: xcode_clt
  changed_when: false
  failed_when: false
  check_mode: false
  when: ansible_facts.os_family == 'Darwin'

- name: Require Xcode command line tools
  ansible.builtin.assert:
    that:
      - xcode_clt is not skipped
      - xcode_clt.rc == 0
    fail_msg: "Install compiler tools first: xcode-select --install"
  when: ansible_facts.os_family == 'Darwin'

- name: Install neovim build dependencies
  ansible.builtin.include_tasks:
    file: tasks/_packages.yml
    apply:
      tags: [always]
  vars:
    packages: "{{ neovim_build_packages }}"
    package_label: neovim build dependencies

- name: Ensure neovim source parent exists
  ansible.builtin.file:
    path: "{{ src_root }}"
    state: directory
    mode: "0755"

- name: Ensure neovim install prefix exists
  ansible.builtin.file:
    path: "{{ neovim_prefix }}/bin"
    state: directory
    mode: "0755"

- name: Clone neovim
  ansible.builtin.git:
    repo: "{{ neovim_repo }}"
    dest: "{{ neovim_src }}"
    version: "{{ neovim_git_ref }}"
    update: false

- name: Stat neovim CMake cache
  ansible.builtin.stat:
    path: "{{ neovim_src }}/.deps/CMakeCache.txt"
  register: neovim_deps_cache

- name: Check neovim CMake cache path
  ansible.builtin.command:
    argv:
      - grep
      - -Fq
      - "{{ neovim_src }}"
      - "{{ neovim_src }}/.deps/CMakeCache.txt"
  register: neovim_cmake_path
  changed_when: false
  failed_when: false
  when: neovim_deps_cache.stat.exists

- name: Wipe neovim build dirs if the source tree was moved
  ansible.builtin.file:
    path: "{{ item }}"
    state: absent
  loop:
    - "{{ neovim_src }}/.deps"
    - "{{ neovim_src }}/build"
  when:
    - neovim_deps_cache.stat.exists
    - neovim_cmake_path.rc != 0

- name: Build neovim
  community.general.make:
    chdir: "{{ neovim_src }}"
    params:
      CMAKE_BUILD_TYPE: "{{ neovim_build_type }}"
      CMAKE_INSTALL_PREFIX: "{{ neovim_prefix }}"
  environment: "{{ neovim_build_env }}"

- name: Install neovim
  community.general.make:
    chdir: "{{ neovim_src }}"
    target: install
  environment: "{{ neovim_build_env }}"
